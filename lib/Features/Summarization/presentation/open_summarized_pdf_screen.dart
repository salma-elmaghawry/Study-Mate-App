import 'package:flutter/material.dart';

class OpenSummarizedPdfScreen extends StatelessWidget {
  const OpenSummarizedPdfScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
